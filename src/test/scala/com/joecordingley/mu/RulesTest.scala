package com.joecordingley.mu

import org.scalatest.{FreeSpec, Matchers}

class RulesTest extends FreeSpec with Matchers {
  val fivePlayers = (1 to 5).toList.map(Player(_))
  val playerHands: List[(Player, InitialHand)] = 

  "during the bidding stage, a player" - {
    "when it is their turn" - {

    }
    "when it is not their turn" - {
      "should not be allowed to bid in" {

      }
    }

  }

}
